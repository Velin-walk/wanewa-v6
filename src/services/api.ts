import { Trek } from "../types";const configuredBaseUrl = (import.meta as ImportMeta & { env?: { PROD?: boolean; VITE_API_BASE_URL?: string } }).env;export const API_BASE_URL = (configuredBaseUrl?.VITE_API_BASE_URL?.trim() || (configuredBaseUrl?.PROD ? "https://walk-nepal-walk-api.velinrai-vr.workers.dev" : "/api")).replace(/\/+$/, "");export function apiUrl(path: string): string { return `${API_BASE_URL}/${path.replace(/^\/+/, "")}`; }export function apiFetch(path: string, options?: RequestInit): Promise<Response> { return fetch(apiUrl(path), options); }export function normalizeTrek(row: any): Trek { const difficulty = String(row.difficulty || "Easy").toLowerCase(); return { id: String(row.id || row.hike_number || row.trek_id || row.trek_name || row.name), hike_number: String(row.hike_number || ""), name: row.name || row.trek_name || "", date: row.date || "", days: row.days || "1", difficulty: difficulty === "hard" ? "difficult" : difficulty === "moderate" ? "moderate" : "easy", leader: row.leader || row.team_leader || "Walk Nepal Walk Guide", capacity: Number(row.capacity || row.max_capacity) || 25, participants: Number(row.participants ?? row.registered_pax) || 0, participants_by_gender: row.participants_by_gender, recent_participants: row.recent_participants, itinerary_link: row.itinerary_link || "", faq_link: row.faq_link || "", whatsapp_link: row.whatsapp_link || "", price: row.price || "", featured_image: row.featured_image || row.thumbnail_url || "", fitness_level: row.fitness_level || "All fitness levels", season: row.season || "Autumn / Year-round", type_of_trail: row.type_of_trail || "", start_location: row.start_location || "", elevation: row.elevation || "", itinerary: row.itinerary || "" }; }
